import styles from './layout.module.css'

export default function Layout() {
  return (
    <div>
      {/* Add your elements here, make sure to break it to clear functions*/}
    </div>
  )
}

Layout.defaultProps = {}
